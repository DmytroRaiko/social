const header = require('./Header');
const buttons = require('./Buttons');
const forms = require('./Forms');
const modals = require('./Modals');
const textFieldMessages = require('./TextFieldMessages');
const messages = require('./Messages');

module.exports = {
  header,
  buttons,
  forms,
  modals,
  textFieldMessages,
  messages,
};
